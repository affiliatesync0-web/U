
rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    match /{allPaths=**} {
      // PERMITIR ACCESO TOTAL PARA PROTOTIPADO
      // IMPORTANTE: En producción, restringe esto a usuarios autenticados.
      allow read, write: if true;
    }
  }
}
